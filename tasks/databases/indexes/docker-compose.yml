version: '3.8'

services:
  db:
    image: postgres:latest
    restart: always
    ports: ['5432:5432']
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb

  adminer:
    image: adminer
    restart: always
    ports: ['8888:8080']
